/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false, // Disabled for now to ease migration of effects
    images: {
        unoptimized: true,
    },
}

export default nextConfig
